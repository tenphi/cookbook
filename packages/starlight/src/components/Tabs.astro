---
import { TabsRoot } from "./TastyComponents.js";

interface Props {
  label?: string;
}
const { label = "Examples" } = Astro.props;
---

<TabsRoot
  className="td-tabs"
  role="group"
  aria-label={label}
  data-tasty-anatomy="Tabs"
>
  <slot />
</TabsRoot>
