---
import { content } from "virtual:cookbook/config";
import { HeaderLinksRoot } from "./LayoutComponents.js";

const links = content.site.headerLinks ?? [];
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const href = (link: string) => (link.startsWith("/") ? `${base}${link}` : link);
---

{
  links.length > 0 && (
    <HeaderLinksRoot data-tasty-anatomy="HeaderLinks">
      <nav class="td-header-links__desktop" aria-label="Header links">
        {links.map((link) => (
          <a
            href={href(link.link)}
            data-variant={link.variant}
            target={link.newTab ? "_blank" : undefined}
            rel={link.newTab ? "noopener noreferrer" : undefined}
          >
            {link.label}
          </a>
        ))}
      </nav>
      <button
        type="button"
        class="td-header-links__trigger"
        popovertarget="cookbook-header-links"
        aria-label="More"
        title="More"
      >
        <svg
          viewBox="0 0 24 24"
          width="20"
          height="20"
          aria-hidden="true"
          fill="currentColor"
        >
          <>
            <circle cx="12" cy="5" r="2" />
            <circle cx="12" cy="12" r="2" />
            <circle cx="12" cy="19" r="2" />
          </>
        </svg>
      </button>
      <div
        id="cookbook-header-links"
        popover="auto"
        class="td-header-links__panel"
      >
        <button
          type="button"
          class="td-header-links__close"
          popovertarget="cookbook-header-links"
          popovertargetaction="hide"
          aria-label="Close more menu"
        >
          <svg viewBox="0 0 24 24" width="20" height="20" aria-hidden="true">
            <path
              d="m6 6 12 12M6 18 18 6"
              fill="none"
              stroke="currentColor"
              stroke-width="1.5"
              stroke-linecap="round"
            />
          </svg>
        </button>
        <nav aria-label="More links">
          {links.map((link) => (
            <a
              href={href(link.link)}
              target={link.newTab ? "_blank" : undefined}
              rel={link.newTab ? "noopener noreferrer" : undefined}
            >
              {link.label}
            </a>
          ))}
        </nav>
      </div>
    </HeaderLinksRoot>
  )
}
