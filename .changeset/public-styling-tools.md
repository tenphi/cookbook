---
"@tenphi/cookbook": minor
"@tenphi/starlight": minor
---

Expose Tasty component and global styling tools through `/styling`, including
typed style objects and helpers that merge `theme.styles` into consumer-owned
components. Document a custom site title with an aligned, linked logo and
verify CSS extraction from a clean package installation.
